# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env.local
.env.development.local
.env.test.local
.env.production.local
# for prisma
.env

# vercel
.vercel

# yarn - no zero installs for now
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.pnp.*

# include node_modules for cases that need node_modules
# note that this is not the same as .yarn/unplugged/
# /node_modules

# ignore prisma since the client is built with private URLS
# prisma client has been made its own private repo
prisma